name: storage
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^3.1.0
  flutter_secure_storage: ^11.2.0
  path_provider: ^2.1.6
  hydrated_bloc: ^11.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
