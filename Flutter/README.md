# عالم المراتب — Flutter Monorepo

Workspace root (`pubspec.yaml`) + Melos scripts. Apps: `staff` (POS+inventory+delivery), `customer`, `admin`.

## Commands

```sh
melos analyze
melos gen
melos test
melos format
```
